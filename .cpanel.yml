# deployment:
#   tasks:
#     - export DEPLOYPATH=/home/region11/davdeoro.region11.dilg.gov.ph
#     - /bin/cp -R admin css database images includes uploads *.php $DEPLOYPATH

deployment:
  tasks:
    - export DEPLOYPATH=/home/region11/davdeoro.region11.dilg.gov.ph
    - /bin/cp -a .htaccess $DEPLOYPATH
    - /bin/cp -a admin css database images includes uploads $DEPLOYPATH
    - /bin/cp -a *.php $DEPLOYPATH